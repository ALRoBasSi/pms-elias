# دليل البنية التقنية - محل الصدارة

## 📋 نظرة عامة

نظام إدارة منتجات الصدارة هو تطبيق ويب متطور يعمل بالكامل في المتصفح، مصمم لإدارة المنتجات والمخزون ونقطة البيع. النظام مبني باستخدام تقنيات الويب الحديثة ويوفر تجربة مستخدم متقدمة مع إمكانيات تخصيص شاملة.

---

## 🏗️ البنية التقنية

### التقنيات الأساسية
- **HTML5**: هيكل الصفحات والعناصر
- **CSS3**: التصميم والتنسيق المتقدم
- **JavaScript ES6+**: منطق التطبيق والتفاعل
- **Bootstrap 5.3.2**: إطار العمل للواجهة
- **IndexedDB**: قاعدة البيانات المحلية

### المكونات الرئيسية
- **إدارة المنتجات**: إضافة، تعديل، حذف، وعرض المنتجات
- **نظام التقارير**: تقارير شاملة وقابلة للتصدير
- **نقطة البيع**: نظام بيع متكامل مع فواتير
- **نظام التنبيهات**: تنبيهات داخلية وخارجية
- **إدارة الإعدادات**: تخصيص شامل للنظام

---

## 📁 هيكل الملفات

```
ALSadara-Products/
├── index.html              # الصفحة الرئيسية
├── css/
│   └── styles.css          # ملف التنسيقات الرئيسي
├── js/
│   └── app.js              # منطق التطبيق الرئيسي
├── products/
│   └── index.html          # صفحة إدارة المنتجات
├── pos/
│   └── index.html          # صفحة نقطة البيع
├── reports/
│   └── index.html          # صفحة التقارير
├── settings/
│   └── index.html          # صفحة الإعدادات
├── CHANGELOG.md            # سجل التغييرات
├── TEST_CASES.md           # دليل اختبارات القبول
└── ARCHITECTURE.md         # هذا الملف
```

---

## 🗄️ قاعدة البيانات (IndexedDB)

### هيكل قاعدة البيانات

#### جدول المنتجات (products)
```javascript
{
  id: String,                    // معرف فريد
  name_ar: String,              // الاسم بالعربية
  sku: String,                  // رمز المنتج
  model: String,                // الموديل
  brand: String,                // الماركة
  price_usd: Number,            // السعر بالدولار
  stock: Number,                // الكمية في المخزون
  mainCategory: String,         // الفئة الرئيسية
  subCategory: String,          // الفئة الفرعية
  extraFieldValues: Object,     // القيم الديناميكية
  createdAt: Date,              // تاريخ الإنشاء
  updatedAt: Date               // تاريخ آخر تحديث
}
```

#### جدول الفواتير (invoices)
```javascript
{
  id: String,                   // معرف الفاتورة
  invoiceNumber: String,        // رقم الفاتورة
  customerName: String,         // اسم العميل
  items: Array,                 // المنتجات المباعة
  subtotal: Number,             // المجموع الفرعي
  taxRate: Number,              // نسبة الضريبة
  taxAmount: Number,            // مبلغ الضريبة
  total: Number,                // المجموع الإجمالي
  paymentMethod: String,        // طريقة الدفع
  createdAt: Date               // تاريخ البيع
}
```

#### جدول سجل العمليات (auditLogs)
```javascript
{
  id: String,                   // معرف السجل
  operation: String,            // نوع العملية
  details: Object,              // تفاصيل العملية
  userId: String,               // معرف المستخدم
  timestamp: Date               // وقت العملية
}
```

### فهارس قاعدة البيانات
- **فهرس الاسم**: للبحث السريع في أسماء المنتجات
- **فهرس الفئة**: لفلترة المنتجات حسب الفئة
- **فهرس السعر**: للبحث حسب نطاق السعر
- **فهرس التاريخ**: لترتيب المنتجات حسب التاريخ

---

## 🔧 المكونات البرمجية

### الكائن الرئيسي (app)

#### الخصائص الأساسية
```javascript
const app = {
  // إعدادات التطبيق
  config: {
    version: '2.0.0',
    debug: false,
    autoBackup: true
  },
  
  // عناصر DOM
  elements: {
    // العناصر الأساسية
    container: null,
    productModal: null,
    detailModal: null,
    
    // عناصر الصفحات
    productsContainer: null,
    reportsContainer: null,
    posContainer: null
  },
  
  // حالة التطبيق
  state: {
    currentPage: 'products',
    selectedProduct: null,
    formStructure: null,
    cart: []
  }
}
```

#### الدوال الرئيسية
- **`init()`**: تهيئة التطبيق
- **`loadAndRenderProducts()`**: تحميل وعرض المنتجات
- **`handleFormSubmit()`**: معالجة إرسال النماذج
- **`deleteProduct()`**: حذف المنتج
- **`generateReport()`**: إنشاء التقارير

### نظام التنبيهات (notificationSystem)

#### الخصائص
```javascript
const notificationSystem = {
  settings: {
    inAppNotifications: true,      // التنبيهات الداخلية
    browserNotifications: false,   // إشعارات المتصفح
    soundEnabled: false           // الصوت
  },
  
  // دعم الإشعارات
  browserSupport: false,
  permission: 'default'
}
```

#### الوظائف الرئيسية
- **`init()`**: تهيئة نظام التنبيهات
- **`showInAppNotification()`**: عرض تنبيه داخلي
- **`showBrowserNotification()`**: عرض إشعار المتصفح
- **`requestPermission()`**: طلب إذن الإشعارات

### نظام تسجيل العمليات (auditLogger)

#### الخصائص
```javascript
const auditLogger = {
  maxLogs: 1000,                 // الحد الأقصى للسجلات
  logTypes: [                    // أنواع العمليات
    'add_product',
    'update_product',
    'delete_product',
    'create_invoice'
  ]
}
```

#### الوظائف الرئيسية
- **`logOperation()`**: تسجيل عملية جديدة
- **`getLogs()`**: استرجاع السجلات
- **`clearLogs()`**: مسح السجلات

---

## 🎨 نظام التصميم

### الألوان والثيمات
```css
:root {
  /* الألوان الأساسية */
  --primary-color: #007bff;
  --success-color: #28a745;
  --warning-color: #ffc107;
  --danger-color: #dc3545;
  --info-color: #17a2b8;
  
  /* الألوان الثانوية */
  --light-color: #f8f9fa;
  --dark-color: #343a40;
  
  /* ألوان النصوص */
  --text-primary: #212529;
  --text-secondary: #6c757d;
  --text-muted: #868e96;
}
```

### الثيم المظلم
```css
[data-theme="dark"] {
  --bg-primary: #1a1a1a;
  --bg-secondary: #2d2d2d;
  --text-primary: #ffffff;
  --text-secondary: #b0b0b0;
}
```

### المكونات المخصصة
- **بطاقات المنتجات**: تصميم متجاوب مع معلومات شاملة
- **النوافذ المنبثقة**: تصميم حديث مع تأثيرات سلسة
- **أشرطة التقدم**: مؤشرات بصرية للعمليات الطويلة
- **الجداول التفاعلية**: فرز وتصفية متقدمة

---

## 📊 نظام التقارير

### أنواع التقارير المتاحة

#### 1. تقرير المبيعات
- **البيانات**: المبيعات حسب الفترة والفئة
- **الإحصائيات**: المجموع، عدد الفواتير، متوسط الفاتورة
- **الفلاتر**: التاريخ، الفئة، نطاق السعر

#### 2. تقرير أفضل المنتجات
- **البيانات**: المنتجات الأكثر مبيعاً
- **المعايير**: حسب الكمية أو الإيرادات
- **الترتيب**: تنازلي مع النسب المئوية

#### 3. تقرير المخزون المنخفض
- **البيانات**: المنتجات التي تحتاج إعادة طلب
- **المعايير**: الكمية الحالية مقابل الحد الأدنى
- **الأولوية**: حسب مستوى المخزون

#### 4. تقرير حركة المخزون
- **البيانات**: تتبع عمليات البيع والإضافة
- **الفترة**: حسب التاريخ المحدد
- **التفاصيل**: المنتج، الكمية، التاريخ، النوع

### محرك التقارير
```javascript
const reportEngine = {
  // إنشاء التقرير
  generateReport(type, filters) {
    // معالجة الفلاتر
    // استعلام قاعدة البيانات
    // تحليل البيانات
    // إرجاع النتائج
  },
  
  // تصدير البيانات
  exportReport(data, format) {
    // تحويل البيانات للتنسيق المطلوب
    // إنشاء الملف
    // تحميل الملف
  }
}
```

---

## 🛒 نظام نقطة البيع

### المكونات الرئيسية

#### 1. عرض المنتجات
- **البحث السريع**: بحث فوري مع debouncing
- **فلتر الفئات**: تصفية حسب الفئة
- **العرض المتجاوب**: شبكة أو قائمة

#### 2. السلة الذكية
- **الإضافة السريعة**: نافذة منبثقة للكمية والسعر
- **التحديث المباشر**: حساب الضريبة تلقائياً
- **التعديل المرن**: تغيير الكمية أو السعر

#### 3. نظام الفواتير
- **الترقيم التلقائي**: حسب الإعدادات
- **معلومات المتجر**: قابلة للتخصيص
- **الطباعة**: تنسيق محسن للطباعة

### إعدادات نقطة البيع
```javascript
const posSettings = {
  // معلومات المتجر
  storeInfo: {
    name: 'محل الصدارة',
    address: 'العنوان',
    phone: 'الهاتف',
    taxNumber: 'رقم الضريبة'
  },
  
  // إعدادات الفاتورة
  invoice: {
    prefix: 'INV',
    digits: 6,
    resetPeriod: 'monthly',
    taxRate: 0.15
  },
  
  // إعدادات الطباعة
  print: {
    width: '80mm',
    fontSize: '12px',
    showBarcode: true,
    showSupplier: false
  }
}
```

---

## ⚙️ نظام الإعدادات

### تبويبات الإعدادات

#### 1. إعدادات التقارير
- **تفعيل التقارير**: اختيار التقارير المرغوبة
- **إعدادات التصدير**: تنسيق الملفات
- **التقارير الدورية**: جدولة تلقائية

#### 2. إعدادات نقطة البيع
- **معلومات المتجر**: بيانات الاتصال والعنوان
- **ترقيم الفواتير**: نظام الترقيم المخصص
- **إعدادات الطباعة**: تنسيق الفواتير

#### 3. إعدادات التنبيهات
- **التنبيهات الداخلية**: داخل التطبيق
- **إشعارات المتصفح**: نظام التشغيل
- **الصوت**: تنبيهات صوتية

### تخزين الإعدادات
```javascript
// localStorage keys
const STORAGE_KEYS = {
  NOTIFICATION_SETTINGS: 'notificationSettings',
  REPORTS_SETTINGS: 'reportsSettings',
  POS_SETTINGS: 'posSettings',
  THEME_SETTINGS: 'themeSettings',
  AUDIT_LOGS: 'auditLogs'
}
```

---

## 🔄 إدارة الحالة

### حالة التطبيق
```javascript
const appState = {
  // الصفحة الحالية
  currentPage: 'products',
  
  // المنتج المحدد
  selectedProduct: null,
  
  // هيكل النموذج
  formStructure: null,
  
  // سلة التسوق
  cart: [],
  
  // الفلاتر النشطة
  activeFilters: {
    category: 'all',
    priceRange: [0, 1000],
    dateRange: null
  }
}
```

### تحديث الحالة
```javascript
// تحديث الحالة
const updateState = (newState) => {
  Object.assign(appState, newState);
  notifyStateChange();
};

// إشعار التغييرات
const notifyStateChange = () => {
  // تحديث الواجهة
  // حفظ الحالة
  // إشعار المكونات
};
```

---

## 🚀 الأداء والتحسين

### استراتيجيات الأداء

#### 1. تحميل البيانات
- **التحميل التدريجي**: تحميل البيانات حسب الحاجة
- **الذاكرة التخزينية**: تخزين البيانات المستخدمة
- **التحسين الذكي**: تحسين الاستعلامات

#### 2. البحث والفلترة
- **Debouncing**: تأخير البحث 200ms
- **الفهرسة**: استخدام فهارس قاعدة البيانات
- **التحسين**: تجنب البحث في جميع البيانات

#### 3. الذاكرة
- **إدارة الذاكرة**: تنظيف الذاكرة تلقائياً
- **تحسين الكائنات**: تقليل استهلاك الذاكرة
- **المراقبة**: مراقبة استخدام الذاكرة

### مؤشرات الأداء
```javascript
const performanceMetrics = {
  // أوقات الاستجابة
  responseTime: {
    search: 200,        // ms
    render: 100,        // ms
    export: 500         // ms
  },
  
  // استخدام الذاكرة
  memoryUsage: {
    maxProducts: 1000,  // منتج
    maxLogs: 1000,      // سجل
    cacheSize: 50       // MB
  }
}
```

---

## 🔒 الأمان والحماية

### حماية البيانات

#### 1. التحقق من الصحة
```javascript
const validation = {
  // التحقق من النصوص
  validateText: (text, minLength, maxLength) => {
    return text && text.length >= minLength && text.length <= maxLength;
  },
  
  // التحقق من الأرقام
  validateNumber: (number, min, max) => {
    return !isNaN(number) && number >= min && number <= max;
  },
  
  // التحقق من التواريخ
  validateDate: (date) => {
    return date instanceof Date && !isNaN(date);
  }
};
```

#### 2. حماية من الهجمات
- **XSS**: تنظيف المدخلات
- **Injection**: التحقق من البيانات
- **CSRF**: حماية النماذج

### النسخ الاحتياطية
```javascript
const backupSystem = {
  // نسخ احتياطية تلقائية
  autoBackup: {
    interval: 24 * 60 * 60 * 1000,  // 24 ساعة
    maxBackups: 7,                  // 7 نسخ
    compress: true                  // ضغط الملفات
  },
  
  // استعادة البيانات
  restore: (backupData) => {
    // التحقق من صحة البيانات
    // استعادة البيانات
    // تحديث الفهارس
  }
};
```

---

## 📱 التوافق والمتصفحات

### المتصفحات المدعومة
- **Chrome**: 90+ ✅
- **Firefox**: 88+ ✅
- **Safari**: 14+ ✅
- **Edge**: 90+ ✅

### الميزات المطلوبة
- **IndexedDB**: قاعدة البيانات المحلية
- **LocalStorage**: تخزين الإعدادات
- **Notification API**: إشعارات المتصفح
- **ES6 Modules**: الوحدات الحديثة

### الاختبار والتحقق
```javascript
const browserSupport = {
  // التحقق من دعم IndexedDB
  checkIndexedDB: () => {
    return 'indexedDB' in window;
  },
  
  // التحقق من دعم الإشعارات
  checkNotifications: () => {
    return 'Notification' in window;
  },
  
  // التحقق من دعم ES6
  checkES6: () => {
    try {
      new Function('(a = 0) => a');
      return true;
    } catch (e) {
      return false;
    }
  }
};
```

---

## 🔧 التطوير والصيانة

### أدوات التطوير
- **VS Code**: محرر الكود
- **Chrome DevTools**: تصحيح الأخطاء
- **Lighthouse**: تحليل الأداء
- **ESLint**: فحص الكود

### معايير الكود
```javascript
// تسمية المتغيرات
const camelCase = 'variable';
const CONSTANT_CASE = 'constant';

// تسمية الدوال
const functionName = () => {
  // منطق الدالة
};

// تسمية الكلاسات
class ClassName {
  constructor() {
    // البناء
  }
}
```

### إدارة الإصدارات
- **Git**: نظام التحكم في الإصدارات
- **Semantic Versioning**: تسمية الإصدارات
- **Changelog**: سجل التغييرات
- **Releases**: إصدارات مستقرة

---

## 📈 التطوير المستقبلي

### الميزات المخططة
- **دعم الباركود**: مسح وعرض الباركود
- **التقارير المتقدمة**: تحليلات أكثر تفصيلاً
- **الإشعارات بالإيميل**: إرسال التقارير بالإيميل
- **النسخ الاحتياطية السحابية**: تخزين سحابي

### التحسينات التقنية
- **Service Worker**: العمل دون اتصال
- **Web Workers**: معالجة البيانات في الخلفية
- **Progressive Web App**: تطبيق ويب تقدمي
- **Responsive Design**: تصميم متجاوب محسن

---

## 📚 التوثيق والدعم

### مصادر التعلم
- **MDN Web Docs**: وثائق الويب
- **Bootstrap Documentation**: دليل Bootstrap
- **IndexedDB Guide**: دليل قاعدة البيانات
- **JavaScript ES6+**: دليل JavaScript الحديث

### الدعم الفني
- **التوثيق الداخلي**: تعليقات في الكود
- **دليل المستخدم**: تعليمات الاستخدام
- **اختبارات القبول**: سيناريوهات الاختبار
- **سجل التغييرات**: تتبع التطوير

---

*تم إعداد هذا الدليل لتوفير فهم شامل للبنية التقنية لنظام إدارة منتجات الصدارة، مع التركيز على القابلية للصيانة والتطوير المستقبلي.*
