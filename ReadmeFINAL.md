# تقرير وتوثيق شامل لمشروع "نظام الصدارة لإدارة المنتجات"

## 📜 مقدمة

هذا المستند يقدم تحليلاً شاملاً لمشروع **ALSadara-Products**، وهو نظام متكامل لإدارة المنتجات والمخزون ونقاط البيع. تم بناء هذا النظام ليعمل بشكل كامل من جانب العميل (Client-Side)، معتمداً على تقنيات الويب الحديثة لتوفير تجربة استخدام سريعة، آمنة، وقابلة للتخصيص. يتم تخزين جميع البيانات محلياً في متصفح المستخدم باستخدام قاعدة بيانات `IndexedDB`، مما يجعله حلاً مثالياً للمتاجر الصغيرة والمتوسطة التي لا تحتاج إلى بنية تحتية معقدة.

---

## 🌟 الوظائف والميزات الأساسية

يتميز النظام بمجموعة من الوحدات المترابطة التي توفر حلاً متكاملاً لإدارة المحل التجاري:

1.  **إدارة المنتجات (Products Management):**
    *   إضافة، تعديل، وحذف المنتجات بسهولة.
    *   نظام فئات ديناميكي يسمح بإنشاء حقول مخصصة لكل فئة منتج.
    *   إدارة البراندات (العلامات التجارية) والوحدات (قطعة، كرتون، ...).
    *   نظام تسعير مرن يدعم وحدات قياس مختلفة (مثل السعر بالقطعة، بالمتر، أو باللفة).
    *   إمكانيات بحث وفلترة متقدمة للعثور على المنتجات بسرعة.
    *   استيراد وتصدير بيانات المنتجات بصيغ `JSON` و `CSV`.
     - استيراد وتصدير بيانات المنتجات بصيغ `JSON` و `CSV`.

    3.  **التقارير والإحصائيات (Reports):**
    *   لوحة تحكم تعرض ملخصاً للمبيعات وعدد الفواتير.
    *   تقارير مفصلة عن المبيعات خلال فترة زمنية محددة.
    *   تقارير عن المنتجات الأكثر مبيعاً.
    *   تنبيهات وتقارير عن المنتجات التي وصل مخزونها إلى مستوى منخفض.
    *   تصدير جميع التقارير بصيغ `CSV` و `JSON` أو طباعتها مباشرة.

4.  **الإعدادات المتقدمة (Settings):**
    *   تخصيص كامل لهيكلة نماذج إدخال المنتجات والفئات.
    *   إدارة مركزية للبراندات والوحدات.
    *   إعدادات خاصة بنظام التقارير ونقطة البيع (مثل معلومات المتجر، نسبة الضريبة، ترقيم الفواتير).
    *   التحكم في نظام الإشعارات داخل التطبيق وإشعارات المتصفح.

---

## 📄 تحليل وظائف الصفحات (HTML Files)

يتكون المشروع من عدة صفحات HTML، كل منها يؤدي وظيفة محددة:

### 1. `products/index.html` - صفحة المنتجات
هي الواجهة الرئيسية للتطبيق. وظيفتها الأساسية هي عرض وإدارة جميع المنتجات.
*   **عرض المنتجات:** تُعرض المنتجات على شكل بطاقات (Cards) في شبكة متجاوبة.
*   **البحث والفلترة:** تحتوي على شريط بحث للبحث بالاسم أو الرمز، مع فلاتر لتصفية المنتجات حسب الفئة والبراند، وخيارات لفرز النتائج.
*   **إضافة منتج:** زر "إضافة منتج" يفتح نافذة منبثقة (Modal) تحتوي على نموذج لإدخال كافة تفاصيل المنتج، بما في ذلك الحقول الديناميكية التي تم إنشاؤها في الإعدادات.
*   **تعديل وحذف:** كل بطاقة منتج تحتوي على أزرار لتعديل بيانات المنتج أو حذفه.
*   **عرض التفاصيل:** النقر على بطاقة المنتج يفتح نافذة تعرض جميع تفاصيله بشكل منظم.
*   **استيراد/تصدير:** أزرار لتصدير قائمة المنتجات الحالية أو استيراد قائمة جديدة من ملف.

### 2. `reports/index.html` - صفحة التقارير
توفر هذه الصفحة رؤى تحليلية حول أداء المبيعات والمخزون.
*   **ملخص الأداء:** بطاقات عرض سريعة لأهم المؤشرات (إجمالي المبيعات، عدد الفواتير، إجمالي الضريبة).
*   **فلاتر التقارير:** يمكن للمستخدم تحديد نوع التقرير المطلوب (مبيعات، أفضل المنتجات، ...) وتحديد النطاق الزمني.
*   **عرض البيانات:** يتم عرض نتائج التقرير في جدول منظم.
*   **تصدير وطباعة:** يمكن تصدير بيانات التقرير أو طباعتها.

### 4. `settings/index.html` - صفحة الإعدادات
المركز التحكمي للتطبيق، حيث يمكن للمستخدم تخصيص كل شيء تقريباً.
*   **هيكلة النماذج:** أهم ميزة في النظام، تسمح للمستخدم بإنشاء فئات رئيسية للمنتجات وتحديد الحقول الخاصة بكل فئة (نص، رقم، قائمة منسدلة، ...).
*   **إدارة البراندات والوحدات:** واجهات بسيطة لإضافة وتعديل وحذف البراندات والوحدات المستخدمة في التطبيق.
*   **إعدادات التقارير ونقطة البيع:** أقسام مخصصة لتحديد معلومات المتجر التي تظهر في الفاتورة، نسبة الضريبة، طريقة ترقيم الفواتير، وغيرها.
*   **إعدادات الإشعارات:** للتحكم في كيفية ووقت ظهور التنبيهات.

### 5. `index.html` (الملف الجذري)
ملف بسيط وظيفته الوحيدة هي إعادة توجيه المستخدم تلقائياً إلى الصفحة الرئيسية الفعلية (`products/index.html`).

---

## 🛠️ تحليل الملفات التقنية

*   **`js/app.js`:**
    هذا هو العقل المدبر للتطبيق. ملف JavaScript ضخم ومنظم يحتوي على كل المنطق البرمجي. هو مقسم إلى كائنات (Objects) لتسهيل إدارته:
    *   `dbManager`: مسؤول عن كل عمليات قاعدة البيانات `IndexedDB` (إضافة، قراءة، تحديث، حذف).
    *   `notificationSystem`: يدير جميع أنواع الإشعارات.
    *   `auditLogger`: يسجل العمليات المهمة التي يقوم بها المستخدم.
    *   `utils`: يحتوي على دوال مساعدة متفرقة (مثل توليد ID فريد، تحويل JSON إلى CSV).
    *   `app`: الكائن الرئيسي الذي يربط كل شيء ببعضه ويدير واجهة المستخدم وحالة التطبيق.
    التعليقات التفصيلية باللغة العربية داخل هذا الملف تجعل فهمه وصيانته أمراً يسيراً.

*   **`css/styles.css`:**
    يحتوي على التنسيقات المخصصة التي تُضاف فوق إطار العمل Bootstrap. يتضمن تعريفات الوضع المظلم والفاتح (`dark/light mode`)، وتصميم بطاقات المنتجات، وتنسيق النماذج والنوافذ المنبثقة.

*   **ملفات Markdown (`.md`):**
    *   `ARCHITECTURE.md`: وثيقة معمارية النظام، تشرح بالتفصيل البنية التقنية وهيكل قاعدة البيانات.
    *   `CHANGELOG.md`: سجل يوثق التغييرات والميزات الجديدة في الإصدارات المختلفة.
    *   `INVOICE_TEMPLATES.md`: يحتوي على قوالب HTML و CSS جاهزة لتصاميم الفواتير المختلفة.
    *   `TEST_CASES.md`: دليل شامل لحالات الاختبار لضمان جودة البرنامج.
    *   `Readme.md`: ملف القراءة العام للمشروع.

---

## 🏁 خلاصة

مشروع **"نظام الصدارة لإدارة المنتجات"** هو مثال ممتاز على تطبيق ويب حديث وقوي يعمل بالكامل في المتصفح. إنه مصمم بعناية، مع توثيق شامل باللغة العربية، وهيكلية برمجية قابلة للتطوير والتوسعة. اعتماده على `IndexedDB` يجعله حلاً فعالاً من حيث التكلفة، بينما توفر لوحة الإعدادات مرونة عالية للمستخدم النهائي لتكييف النظام حسب احتياجاته الخاصة.

---

## 🌐 تشغيل المشروع بدون اتصال بالإنترنت (Offline)

يعتمد المشروع حالياً على شبكة الإنترنت لتحميل ملفات `Bootstrap` و `Bootstrap Icons` من خلال شبكة توصيل المحتوى (CDN). لكي يعمل المشروع بشكل كامل دون الحاجة إلى اتصال بالإنترنت، يجب تحميل هذه الملفات وتضمينها محلياً.

اتبع الخطوات التالية:

### الخطوة 1: تحميل ملفات Bootstrap (CSS & JS)

1.  اذهب إلى [صفحة تحميل Bootstrap](https://getbootstrap.com/docs/5.3/getting-started/download/).
2.  ابحث عن قسم **"Compiled CSS and JS"** وانقر على زر **Download**.
3.  من الملف المضغوط الذي تم تحميله، انسخ الملفات التالية:
    *   `css/bootstrap.rtl.min.css` -> والصقه في مجلد `css` بمشروعك.
    *   `js/bootstrap.bundle.min.js` -> والصقه في مجلد `js` بمشروعك.

### الخطوة 2: تحميل وتشغيل أيقونات Bootstrap (Bootstrap Icons)

الأيقونات تحتاج إلى ملف CSS **بالإضافة إلى ملفات الخطوط الخاصة بها** لتعمل بشكل صحيح.

1.  اذهب إلى [صفحة Bootstrap Icons](https://icons.getbootstrap.com/) وابحث عن قسم **Install**.
2.  انقر على زر **Download** لتحميل الملف المضغوط الخاص بالأيقونات.
3.  افتح الملف المضغوط. ستجد بداخله ملف `bootstrap-icons.min.css` ومجلد اسمه `fonts`.
4.  **نسخ الملف والمجلد:**
    *   انسخ ملف `bootstrap-icons.min.css` وألصقه داخل مجلد `css` في مشروعك.
    *   انسخ **مجلد `fonts` بالكامل** وألصقه في **المجلد الرئيسي** لمشروعك (بجانب مجلدات `css` و `js`).

    يجب أن يبدو هيكل المجلدات لديك هكذا:
    ```
    ALSadara-Products/
    ├── css/
    │   └── bootstrap-icons.min.css
    ├── fonts/  <-- المجلد الجديد الذي يحتوي على ملفات .woff و .woff2
    │   ├── bootstrap-icons.woff
    │   └── bootstrap-icons.woff2
    └── js/
    ```

### الخطوة 3: تعديل ملفات HTML

الآن، يجب تعديل جميع ملفات الـ HTML في المشروع (`products/index.html`, `reports/index.html`, `settings/index.html`) لاستخدام الملفات المحلية بدلاً من روابط CDN.

**1. استبدل رابط CSS الخاص بـ Bootstrap:**

ابحث عن السطر التالي:
```html
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
```
واستبدله بالسطر التالي:
```html
<link href="../css/bootstrap.rtl.min.css" rel="stylesheet">
```
*(ملاحظة: المسار `../css/` صحيح لأن ملفات HTML موجودة داخل مجلدات فرعية مثل `products` و `reports` و `settings`)*.

**2. استبدل رابط CSS الخاص بالأيقونات:**

ابحث عن السطر التالي:
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
```
واستبدله بالسطر التالي:
```html
<link rel="stylesheet" href="../css/bootstrap-icons.min.css">
```

**3. استبدل رابط JavaScript الخاص بـ Bootstrap:**

ابحث عن السطر التالي في نهاية الملف:
```html
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
```
واستبدله بالسطر التالي:
```html
<script src="../js/bootstrap.bundle.min.js"></script>
```

بعد تطبيق هذه التغييرات على جميع صفحات HTML، سيصبح المشروع قادراً على العمل بشكل كامل دون الحاجة إلى اتصال بالإنترنت، وستظهر الأيقونات بشكل صحيح.